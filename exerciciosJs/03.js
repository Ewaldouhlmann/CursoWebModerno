const elevation = (base, exponent) => {
    /*This function receives two int values (base and exponent) and returns a expoential function with base and
    exponent defined*/
    return base ** exponent;
};

console.log(elevation(4, 4))